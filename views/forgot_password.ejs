<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password reset</title>
    <style>
        :root {
            --accent-color: #f89a1a;
            --black-neutral-color: #12130c;
            --white-neutral-color: #f8fbed;
            --complementary-color: #1c45e3;
            --complementary-background-color: #7e95ef;
            --yellow-color: #f5e50c;
        }

        body {
            width: 100vw;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: var(--white-neutral-color);
            overflow: hidden;
        }

        .container {
            width: 480px;
            background-color: white;
            padding: 4rem;
        }

        input {
            font-size: 1.2rem;
            width: 100%;
            padding: 5px 15px;
            color: var(--black-neutral-color);
            margin: 1rem 0;
        }

        a,
        button {
            background-color: #f5e50c;
            color: var(--white-neutral-color);
            /* display: inline-block; */
            font-size: 1.2rem;
            border: none;
            outline: none;
            padding: 5px 15px;
            letter-spacing: 2px;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>
            password reset

        </h1>
        <% if (messages.info) { %>
            <div>
                <%= messages.info %>
            </div>
            <% } %>
                <form action="/forgot" method="POST">
                    <input type="text" name="name" id="" placeholder="enter your name">
                    <input type="text" placeholder="enter favourite color" name="color">
                    <button type="submit">submit</button>
                </form>
    </div>

</body>

</html>